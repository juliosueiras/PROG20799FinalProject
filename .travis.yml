language: ruby
before_install: 
  - gem install ceedling
install:
    - mkdir build
    - bundler install
script:
    - bundler exec rake 
    - bundler exec rake test:all
    - bundler exec rake gcov:all


