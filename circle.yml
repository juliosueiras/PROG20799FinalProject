machine:
  ruby:
    version: rbx-2.2.6
general:
    build_dir: build
test: 
    pre:
        - mkdir test/support 
        - mkdir build 
        - gem install ceedling
    post: 
        - bundle exec rake test:all
