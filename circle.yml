machine:
  ruby:
    version: rbx-2.2.6
general:
    build_dir: build
test: 
    pre:
        - mkdir build
        - bundler install
    override:
        - bundler exec rake test:all
    post: 
        - bundler exec rake gcov:all
        - mkdir $CIRCLE_ARTIFACTS/gcov
